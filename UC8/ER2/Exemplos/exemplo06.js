// for (Laço mais utilizado, por ser simples), porém penas permite laços
//contados, ou seja, com contador.

for(contador = 0; contador < 100; contador = contador + 1) // ou contador ++
{
    console.log(contador) 
}

/*
incremento
    contador(Exemplo: contador++) é a mesma coisa que contador = contador + 1

Decremento
            (Exemplo: contador--) é a mesma coisa que contador = contador - 1

/*
let contador = 0; // contador iniciando em 0

while (contador < 100) // precisa de uma condição
{
    console.log(contador)
    contador = contador + 1; //incremento
}
*/